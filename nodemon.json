{
  "watch": ["src"],
  "ext": "ts,js,json,graphql,env,pug",
  "ignore": [".git", "node_modules", "coverage", "dist"],
  "exec": "ts-node -r tsconfig-paths/register ./src/server.ts",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}
